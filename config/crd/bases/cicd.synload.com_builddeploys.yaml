---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: builddeploys.cicd.synload.com
spec:
  group: cicd.synload.com
  names:
    kind: BuildDeploy
    listKind: BuildDeployList
    plural: builddeploys
    singular: builddeploy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BuildDeploy is the Schema for the builddeploys API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BuildDeploySpec defines the desired state of BuildDeploy
            properties:
              build:
                description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file'
                properties:
                  dockerfile:
                    type: string
                  work_dir:
                    type: string
                type: object
              git:
                properties:
                  secret:
                    description: Secret contains a ssh key to connect to private repos
                    type: string
                  uri:
                    type: string
                required:
                - uri
                type: object
              publish:
                properties:
                  host:
                    type: string
                  secret:
                    type: string
                  tag:
                    type: string
                  version:
                    type: string
                required:
                - host
                - secret
                - tag
                - version
                type: object
            required:
            - git
            - publish
            type: object
          status:
            description: BuildDeployStatus defines the observed state of BuildDeploy
            properties:
              built:
                type: boolean
              deployed:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                properties:
                  pod:
                    type: string
                required:
                - pod
                type: object
            required:
            - built
            - deployed
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
